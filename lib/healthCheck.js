
var request = require('request');

module.exports.status = function (req, res) {
    // build this out to do both types of service request and return the
    // relevant status
    res.end('OK');
};
